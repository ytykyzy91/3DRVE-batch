"""Command-line entry for building ML datasets from completed cases."""

from rve_batch.ml.dataset import build_dataset


if __name__ == "__main__":
    raise SystemExit("build_dataset.py scaffold only; implementation pending")
