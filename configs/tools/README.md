# Solver templates

Place solver JSON templates here.

Recommended next step: copy `../../Calculation/user_RVE_analysis.json` into this directory as
`user_RVE_analysis.template.json`, then replace case-specific fields such as `rvePath`
through `src/rve_batch/solver/analysis_config.py`.
